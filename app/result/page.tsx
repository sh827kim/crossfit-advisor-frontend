'use client';

import { ResultPage as ResultPageComponent } from '@/app/components/ResultPage';

export default function ResultPage() {
  // ResultPage 컴포넌트는 이미 setCurrentPage를 사용하므로,
  // 일단 기존 컴포넌트를 사용하고 나중에 리팩토링
  return (
    <ResultPageComponent />
  );
}
